// Shared TypeScript interfaces mirroring backend Pydantic schemas
// (backend/app/schemas/*.py). Add one interface per resource as its schema
// lands, keeping field names/types in lockstep with the Python side.
//
// TODO(Phase 1): User, Workspace, Project, File
// TODO(Phase 4/5): Conversation, Message
// TODO(Phase 8): AgentRun
// TODO(Phase 10): Deployment

export {};
