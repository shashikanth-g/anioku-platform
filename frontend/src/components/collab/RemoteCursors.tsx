// TODO(Phase 6): renders remote collaborators' cursors/selections inside
// MonacoWrapper, backed by useCollabStore + "file:cursor" socket.io events.

export default function RemoteCursors() {
  return null;
}
