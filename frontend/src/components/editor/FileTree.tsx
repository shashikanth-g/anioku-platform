// TODO(Phase 2): recursive file/folder tree, backed by GET /api/v1/files and
// useWorkspaceStore; drives MonacoWrapper's active file via useEditorStore.

export default function FileTree() {
  return null;
}
