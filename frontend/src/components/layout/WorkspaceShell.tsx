// TODO(Phase 2): the IDE's resizable-panel shell composing Sidebar, FileTree,
// EditorTabs + MonacoWrapper, TerminalPanel, AIChatPanel, LivePreview, and
// StatusBar into the workspace/[id] page layout.

export default function WorkspaceShell() {
  return null;
}
