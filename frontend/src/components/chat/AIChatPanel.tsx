// TODO(Phase 4): chat UI streaming from POST /api/v1/ai (or "chat:stream" over
// socket.io), backed by useChatStore. Composes ModelSelector, MessageList, AgentBadge.

export default function AIChatPanel() {
  return null;
}
