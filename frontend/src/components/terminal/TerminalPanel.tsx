// TODO(Phase 3): wraps @xterm/xterm + @xterm/addon-fit, streaming I/O over
// socket.io ("terminal:input"/"terminal:output") to app/runtime/terminal.py,
// backed by useTerminalStore.

export default function TerminalPanel() {
  return null;
}
