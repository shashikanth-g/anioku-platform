// TODO(Phase 2): the IDE shell — composes components/layout/WorkspaceShell with
// the editor, terminal, chat, preview, collab, and git panels.

export default function WorkspacePage({ params }: { params: { id: string } }) {
  return null;
}
