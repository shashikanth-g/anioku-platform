// socket.io-client singleton connecting to NEXT_PUBLIC_SOCKET_URL.
//
// TODO(Phase 3/6): implement
// - a lazily-created, module-scoped Socket instance (auth token in the
//   connection handshake).
// - typed emit/on helpers matching the "domain:action" event naming
//   convention (file:update, terminal:output, presence:join, chat:stream, ...).

export {};
