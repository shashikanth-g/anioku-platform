"""Base agent class shared by all specialized agents.

TODO(Phase 8): implement
- class BaseAgent: name, system_prompt, tools (from app.ai.tools), and an
  async def run(state: dict) -> dict method that each subclass overrides,
  calling app.ai.gateway for model completion.
"""
