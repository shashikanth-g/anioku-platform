"""Conversation ORM model.

TODO(Phase 4/5): id (UUID pk), project_id (fk Project), title, created_at.
Relationship: messages (role, content, model, created_at) — the AI chat panel's
persisted history.
"""
