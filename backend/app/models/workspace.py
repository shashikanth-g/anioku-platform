"""Workspace ORM model.

TODO(Phase 1): id (UUID pk), name, owner_id (fk User), created_at, updated_at.
A workspace is the top-level container a user/team collaborates in; it holds
one or more projects. Relationships: projects, members.
"""
